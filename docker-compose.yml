services:
  backend:
    build: ./backend
    volumes:
      - ./workspace:/data
    environment:
      DISCOGS_BROWSER_DATA: /data
      PLAYWRIGHT_CHANNEL: ""
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped
